node_modules/
.env
.DS_Store
# Tout config/ est du runtime : session, état quotidien, dumps HTML de debug.
# (Le dossier est recréé par le Dockerfile et par le code au besoin.)
config/
*.har
config/seen_logements.TEST.json
